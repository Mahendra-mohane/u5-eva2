{
  "name": "jwt-auth-and-authorisation---ii---only-backend",
  "version": "1.0.0",
  "description": "JWT AUTH AND AUTHORISATION - II - ONLY BACKEND\r Create an app (ONLY BACKEND)\r It should have the following endpoints - /signup, /login, /logout, /products, /addproducts, /deleteproducts.\r Take care of Authentication - Only logged in users should be able to access /products endpoint.\r Take care of Authorisation - Only users who's role is \"seller\", should be able to access /addproducts and /deleteproducts endpoint.\r Store the data in Mongo Database (use Atlas)\r Mandatorily - Take care of all good practices like send status codes, MVC structure, encrypt passwords, use jwt for token etc\r Have refresh tokens and token expiry too in your authentication system, keep the jwt expiry time as 1 minute and refresh token expiry time as 5 minutes\r Have logout endpoint and implement logout using blacklisting.\r Deploy Backend on heroku/render/railway/cyclic etc.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.2.0"
  }
}
